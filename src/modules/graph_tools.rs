pub fn get_sep() -> String {
    let trenner = "================================================================================".to_string();
    trenner
}

pub fn get_error_liner() -> String {
    let erl = "=== FEHLER  bei der Eingabe!           Bitte korrekte Auswahl treffen:       ===".to_string();
    erl
}
